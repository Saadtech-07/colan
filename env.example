# Auth.js / NextAuth — set a strong secret in production
AUTH_SECRET=

# Optional: MongoDB Atlas or local mongodb:// URI. If unset, data lives in server memory only.
MONGODB_URI=
MONGODB_DB=colan_teams

# Optional: canonical URL for Auth.js (e.g. https://colan.example.com)
AUTH_URL=
